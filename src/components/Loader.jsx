export default function Loader() {
  return (
    <div id="loader">
      <div className="osd">◼ TAPE 01 — ASYNC FOUNDATION ARCHIVE</div>
      <button id="playBtn">► PLAY</button>
      <div className="tip osd">HEADPHONES ON — PRESS [F] TO TOGGLE FLASHLIGHT</div>
    </div>
  );
}
